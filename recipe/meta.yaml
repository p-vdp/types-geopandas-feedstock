{% set version = "1.1.3.20260402" %}

package:
  name: types-geopandas
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/t/types-geopandas/types_geopandas-{{ version }}.tar.gz
  sha256: 0e3be3036eeea790e426b90f5ef540b367bb54d57823bf335d4eb70915c28087

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}.*
    - setuptools >=77.0.3
    - pip
  run:
    - python >={{ python_min }}
    - types-shapely
    - numpy >=1.20
    - pandas-stubs
    - pyproj

test:
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}.*

about:
  home: https://github.com/python/typeshed
  summary: Typing stubs for geopandas
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - p-vdp
